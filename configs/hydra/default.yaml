# @package hydra

run:
  dir: ${paths.log_dir}/${now:%Y-%m-%d_%H-%M-%S}

job_logging:
  formatters:
    simple:
      format: '[%(levelname)s] - %(message)s'

sweep:
  dir: ${paths.log_dir}/multirun/${now:%Y-%m-%d_%H-%M-%S}
  subdir: ${hydra.job.num}