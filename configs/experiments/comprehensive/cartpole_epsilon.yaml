# Strategy 3: Fixed Epsilon (50% teacher, 50% student)

experiment:
  name: "cartpole_epsilon"
  seed: 42
  device: "cpu"

environment:
  env_id: "CartPole-v1"
  num_envs: 8

algorithm:
  name: "ppo"
  learning_rate: 3e-4
  n_steps: 2048
  batch_size: 64
  n_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  ent_coef: 0.0
  vf_coef: 0.5
  max_grad_norm: 0.5

scheduler:
  name: "epsilon"
  epsilon: 0.5

teacher:
  name: "optimal"

training:
  total_timesteps: 200000
  eval_freq: 10000
  checkpoint_freq: 50000
  save_freq: 100000

paths:
  log_dir: "logs/comprehensive/cartpole_epsilon"
  checkpoint_dir: "logs/comprehensive/cartpole_epsilon/checkpoints"
  results_dir: "logs/comprehensive/cartpole_epsilon/results"

tracker:
  backends: ["tensorboard", "csv"]
  tensorboard:
    enabled: true
  csv:
    enabled: true
  console:
    enabled: false